# Communication

<% if(RestAPI) { %>
## SYNC

Synchronized communication is configured using REST API.
<% } %>

<% if(RestAPI) { %>```<% } %><% commConfigs.forEach(function(config) { %> <% if(config.client === baseName && config.type === "sync") { %>
communication {
  client "<%= baseName %>"
  server "<%= config.server %>"
  type "sync"
  framework "rest-api"
}
<% } %><% }); %>
<% if(RestAPI) { %>```<% } %>
<% if(RestAPI) { %>
You can call other microservices using the following curl command:
<% } %>

<% commConfigs.forEach(function(config) { %> <% if(config.client === baseName && config.type === "sync") { %>
```
curl http://localhost:<%= serverPort %>/rest/services/<%= config.server  %>
```<% } %> <% }); %>


<% if(RabbitMQ) { %>
## ASYNC

Asynchronous communication pre-configured using RabbitMQ.
<% } %>

<% if(RabbitMQ) { %>```<% } %><% commConfigs.forEach(function(config) { %> <% if(config.server === baseName&& config.type === "async") { %>
communication {
  producer "<%= baseName %>"
  consumer "<%= config.client %>"
  type "async"
  framework "rabbitmq"
}
<% if(RabbitMQ) { %>```<% } %><% } %><% }); %>
